name: mobile_client
description: Shared client models, DTOs, repositories, and services for Carebit.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.9.0

dependencies:
  http: ^1.2.2

dev_dependencies:
  lints: ^6.0.0
